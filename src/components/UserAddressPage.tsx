import React from 'react';

const CreateAddress = () => {
    return (
        <div>
            <h1>Create Address</h1>
            <p>Form to add a new address.</p>
            {/* Add form elements here */}
        </div>
    );
}

export default CreateAddress;